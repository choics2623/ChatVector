# ChatVector
### How to Use
```
CHAT_PATH="adress"
PT_PATH="adress"
CPT_PATH="adress"
SAVE_PATH="adress"

python chat_vecter.py \
     --chat_path "$CHAT_PATH" \
     --pt_path "$CPT_PATH" \
     --aligned_path "$ALIGNED_PATH" \
     --save_path "$SAVE_PATH"
```
